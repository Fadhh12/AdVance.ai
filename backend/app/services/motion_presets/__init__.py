"""Local ffmpeg-based "video-effect generator" (Phase 6R-8) — motion/transition presets
applied to a project's source video/photo entirely with ffmpeg, no paid AI video-gen
provider (none chosen yet, see CLAUDE.md). One module per preset (mirrors
`app/services/ai_providers/`: one file per provider), registered in `registry.py`.
"""
