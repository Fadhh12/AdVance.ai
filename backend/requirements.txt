# adVance.AI backend — pinned to broad, stable ranges (Python 3.11).
# Grouped by SDD §3.1 layer. Add new deps in the right group, keep it sorted.

# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Config / validation ---
# pydantic bumped by google-genai's own requirement (Phase 6R) — verified compatible,
# full test suite green.
pydantic==2.13.5
pydantic-settings==2.7.1
email-validator==2.2.0
python-dotenv==1.0.1

# --- Database (SDD §3.3) ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# --- Auth (FR-01, SDD §3.6 — encrypted tokens) ---
# bcrypt pinned <4.1: passlib 1.7.4's backend-detection breaks on newer bcrypt
# (AttributeError probing bcrypt.__about__) — see https://github.com/pyca/bcrypt/issues/684
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1
authlib==1.4.0
cryptography==44.0.0

# --- Job queue (SDD §3.1 — Celery + Redis) ---
celery==5.4.0
redis==5.2.1

# --- Object storage (S3-compatible: MinIO local / R2 / S3 prod) ---
boto3==1.35.90

# --- AI provider / social publisher HTTP clients (wrapped behind services/) ---
httpx==0.28.1

# --- LLM provider (Phase 6R — agen chat). Both deferred-imported (services/
# llm_providers/factory.py), constructed only when AI_LLM_PROVIDER selects them.
# gemini = default real provider (free tier); anthropic = paid upgrade path later. ---
google-genai==2.22.0
anthropic==1.4.0

# --- Manual Assist exports (Phase 5): caption QR share, video processing ---
qrcode[pil]==8.0
ffmpeg-python==0.2.0

# --- Voice-over provider (Phase 6R-9). Free, no API key — default real provider,
# same "free first" call as google-genai above. Deferred-imported (ai_providers/
# factory.py), only constructed when AI_VOICEOVER_PROVIDER=edge_tts. ---
edge-tts==7.2.8

# --- Dev / test / lint ---
pytest==8.3.4
pytest-asyncio==0.25.0
moto[s3]==5.0.24
ruff==0.8.4
